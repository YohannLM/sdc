/**
 * @file
 * Single Directory Components behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.sdcTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
